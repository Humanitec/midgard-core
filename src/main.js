import { createStore, combineReducers, applyMiddleware, compose } from 'redux';

const Core = {
  redux: {
    createStore,
    combineReducers,
    applyMiddleware,
    compose
  }
};


export default Core;
